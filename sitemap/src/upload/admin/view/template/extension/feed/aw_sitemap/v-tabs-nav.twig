<ul class="nav nav-pills nav-stacked">
    {% for t in tabs %}
        <li class="{{ loop.first ? 'active' }}">
            <a href="#v-tab_{{ t.id }}" data-toggle="tab">{{ t.label }}</a>
        </li>
    {% endfor %}
</ul>
